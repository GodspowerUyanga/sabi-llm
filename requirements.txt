# Core runtime (light). Install with: pip install -r requirements.txt
rich>=13.0
psutil>=5.9
PyYAML>=6.0
numpy>=1.23

# --- Inference backend (required to actually run the model) ---
# llama-cpp-python compiles a small native library. On Ubuntu you may need:
#   sudo apt-get install -y build-essential cmake
llama-cpp-python>=0.2.50

# --- Model download from Hugging Face ---
huggingface_hub>=0.20
