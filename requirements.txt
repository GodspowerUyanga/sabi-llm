# Sabi runtime dependencies — all CPU-only, no GPU, no cloud.
# Pinned loosely for reproducibility on Ubuntu 22.04 / Python 3.10–3.12.

# On-device inference (GGUF, CPU). Builds a CPU wheel; needs build tools the
# first time (setup.sh installs them).
llama-cpp-python>=0.2.90,<0.4

# Web server + streaming UI
fastapi>=0.110,<0.116
uvicorn[standard]>=0.29,<0.35

# Core utilities
numpy>=1.24,<2.3
psutil>=5.9,<7.0
pyyaml>=6.0,<7.0
pydantic>=2.5,<3.0

# Document upload + parsing (ingest-time only; pure-Python, offline)
python-multipart>=0.0.9
pypdf>=4.0,<6.0
python-docx>=1.1,<2.0
openpyxl>=3.1,<4.0

# Model download (download step only; not used at inference)
requests>=2.31,<3.0
# Optional: only needed for the Hugging Face fallback + GGUF metadata rebrand
huggingface-hub>=0.23,<1.0
gguf>=0.9,<0.20
tqdm>=4.66

# Tests
pytest>=8.0
